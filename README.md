# arabic-alphabet-lab

Arabic alphabet as a programmable system: letters, diacritics, joining logic, and analysis.

## Dev setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest
```

### MIT License
Create `LICENSE` (MIT text). 
